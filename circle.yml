machine:
  node:
    version: 0.12.5

  java:
    version: oraclejdk8

deployment:
  staging:
    branch: master
    heroku:
      appname: jhipster-fredrik

general:
  artifacts:
    - "build/libs" # relative to the build directory
