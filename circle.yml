machine:
  java:
    version: oraclejdk8

deployment:
  staging:
    branch: master
    heroku:
      appname: jhipster-fredrik

general:
  artifacts:
    - "build/libs" # relative to the build directory
